/**
 * Set api credentials etc. here.
 * Production keys should be loaded from ENV and NEVER set here, because it will be public in the repo.
 */
export default class Secrets {
  static readonly apiKey: string = 'secret'
}
